import React from 'react'
import { IconArrowDown } from '../Icons/IconArrowDown'

export const IlustrationScrollView = React.memo(() => {
    return (
        <div className="__presentation_ilustration_scroll_view">
            <IconArrowDown />
            <p>Scroll view</p>
        </div>
    )
})
